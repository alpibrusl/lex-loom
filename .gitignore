*.db
# WAL sidecars -- lex-orm opens SQLite in WAL (lex-orm#31), so every database
# here now has two companions.
*.db-wal
*.db-shm
tsconfig.tsbuildinfo
.cli/
llm_call.lex
*-trail.db
.env
ui/node_modules/
ui/dist/

# Company product code lives in $LOOM_WORKSPACE (default ~/loom-companies), never in the loom repo
/projects/
# Build-agent spillage into the tool's cwd (Python scratch, pycache, stray dbs)
__pycache__/
*.pyc
.pytest_cache/
/sqlite::memory:
# Generated benchmark output
/bench/results/

# The eval suite's run-lock. Live state, never source.
evals/.running.pid
