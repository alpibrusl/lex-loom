[package]
name    = "lex-loom"
version = "0.0.0"
edition = "2025"
license = "EUPL-1.2"
description = "Multi-agent sprint cycles built on lex-soft (design stage)"

[dependencies]
"lex-soft"   = { path = "../lex-soft" }
"lex-spec"   = { path = "../lex-spec" }
"lex-schema" = { path = "../lex-schema" }
"lex-jobs"   = { path = "../lex-jobs" }
"lex-mcp"    = { path = "../lex-mcp" }
"lex-agent"  = { path = "../lex-agent" }
"lex-web"    = { path = "../lex-web" }
